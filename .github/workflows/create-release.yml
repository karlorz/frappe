# Disabled - Use custom-release.yml workflow instead
# This workflow is disabled because we use manual Custom Release workflow
# to maintain version parity with upstream releases

name: Generate Semantic Release (DISABLED)
on:
  workflow_dispatch: # Only manual trigger allowed

jobs:
  release:
    name: Release (DISABLED)
    runs-on: ubuntu-latest
    if: false  # Always skip this job
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Use custom-release.yml instead."
